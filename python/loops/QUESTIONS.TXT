These 15 questions cover **`for` loops, `while` loops, `range()`, iteration over strings/lists/tuples, conditions inside loops, counters, accumulators, `break`, `continue`, common loop mistakes, nested loops, and using loops to solve small real-world problems**.

**Q1.**
Write a loop that prints the numbers **1 to 10**, one number on each line.

---

**Q2.**
Create a loop that prints the word `Python` **5 times**.

After writing it, think about why using a loop is better here than writing `print("Python")` five separate times.

---

**Q3.**
Create a list of five names.

Use a loop to print each name from the list one by one.

---

**Q4.**
Use `range()` and a loop to print all the numbers from **1 to 20**.

Then modify your program so that it prints only the numbers from **5 to 15**.

---

**Q5.**
Create a program that prints the **multiplication table of a number** chosen by you.

For example, if the number is `5`, the program should print its table from `5 × 1` through `5 × 10`.

---

**Q6.**
Create a program that calculates the **sum of numbers from 1 to 100**.

Do not calculate the answer manually. Use a loop and a variable to keep track of the total.

---

**Q7.**
A student has these marks:

`[45, 72, 31, 90, 56, 28]`

Use a loop to examine each mark and count how many subjects the student **passed** and how many they **failed**.

Use `33` as the passing mark.

---

**Q8.**
You have a shopping list containing several item names.

Use a loop to display each item with a number before it.

For example, the output should conceptually look like:

`1. Apples`
`2. Milk`
`3. Bread`

The number should be generated by your program rather than manually written.

---

**Q9.**
Create a program that asks the user for a number and keeps asking for another number **until the user enters `0`**.

When `0` is entered, stop taking numbers and display the sum of all the numbers entered before it.

---

**Q10.**
Predict what the following program will do **before running it**:

```python id="x2v7mk"
for i in range(5):
    print(i)
```

What numbers will be printed?

Then change it to print numbers from **1 to 5**.

Finally, explain why `range(5)` does not produce `5`.

---

**Q11.**
Find and fix the mistake in this program:

```python id="c8m1qp"
number = 1

while number <= 5:
    print(number)
```

The programmer wants the program to print:

`1 2 3 4 5`

and then stop.

After fixing it, explain **why the original program does not stop**.

---

**Q12.**
Consider this program:

```python id="r5k3zt"
for number in range(1, 11):
    if number == 6:
        break
    print(number)
```

Predict exactly what will be printed.

Then explain what `break` does to the loop.

After that, change the program so that **6 is skipped but the loop continues printing 7, 8, 9, and 10**.

---

**Q13.**
Create a function that receives a list of numbers and returns how many numbers in the list are **even**.

For example, given:

`[2, 7, 10, 13, 18, 21]`

the function should return the number of even values.

Use a **loop, condition, variable, and function**.

---

**Q14.**
Create a program that takes a word from the user and examines each character.

Count how many characters are:

* vowels
* consonants

Assume the user enters a simple word containing only alphabetic characters.

---

**Q15.**
Create a small **number guessing game**.

The program should have a secret number stored in a variable.

The user repeatedly enters guesses. After each guess, tell the user whether their guess is:

* too high
* too low
* correct

When the user guesses correctly, the program should stop and display a success message.

Decide yourself how to organize the loop, conditions, and variables.