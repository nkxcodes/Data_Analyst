"""
Q10.​
Predict what happens before running this:
numbers = {10, 20, 30}
numbers[0] = 100
print(numbers)
Run it afterward.
Explain why you cannot access or change a set using an index like you can with a list.
"""

numbers = {10, 20, 30}
numbers[0] = 100 

print(numbers)

# Sets are unordered + no indexing.
# Sets don't have positions/indexes like lists do.