These 15 questions cover **creating tuples, accessing elements, indexing, slicing, immutability, tuple methods, membership, unpacking, nesting, loops, conditions, and using tuples in practical programs**.

**Q1.**
Create a tuple containing your **name, age, and city**. Print the entire tuple.

---

**Q2.**
Create a tuple containing five numbers.

Print:

* the first element
* the third element
* the last element

---

**Q3.**
Create a tuple containing the names of five programming languages.

Use indexing to print the name of the **second** and **fourth** languages.

---

**Q4.**
Create a tuple containing numbers from 1 to 10.

Use slicing to create and print:

* the first five numbers
* the last three numbers
* the numbers from position 3 to position 7

---

**Q5.**
Create a tuple containing some repeated numbers, for example:

`(10, 20, 10, 30, 10, 40)`

Find out:

* how many elements are in the tuple
* how many times `10` appears
* the position of the first `30`

Use the appropriate built-in operations or tuple methods.

---

**Q6.**
Create a tuple of five numbers.

Check whether a particular number exists in the tuple.

Then check for a number that does **not** exist.

Your program should clearly tell you whether each number is present.

---

**Q7.**
Imagine a student record is stored as a tuple containing:

* student name
* age
* class
* city

Access each piece of information individually and display it as a readable student profile.

---

**Q8.**
A point on a graph can be represented using a tuple containing two numbers:

`(x, y)`

Create two point tuples.

Then calculate the difference between their x-coordinates and the difference between their y-coordinates.

---

**Q9.**
Create a tuple containing the names of several students.

Use a loop to go through the tuple and display each student's name.

Then use a condition to display only the names that meet a condition you choose, such as names longer than 5 characters.

---

**Q10.**
Predict what will happen when this program runs:

```python
numbers = (10, 20, 30)

numbers[1] = 50

print(numbers)
```

Will Python successfully change the tuple?

Run the program afterward and explain **why** Python behaves this way.

---

**Q11.**
Find and fix the mistake in this code:

```python
student = ("Nitesh", 18, "Delhi")

name, age = student

print(name)
print(age)
```

The programmer wants to store all the information from the tuple in separate variables.

After fixing it, explain why the original code doesn't work.

---

**Q12.**
Predict the output of this program **before running it**:

```python
data = (10, 20, (30, 40), 50)

print(data[2])
print(data[2][0])
```

Then explain how Python is accessing the nested tuple.

---

**Q13.**
Create a tuple containing several numbers.

Use a loop and conditions to count how many numbers are:

* even
* odd

Do not change the original tuple.

---

**Q14.**
Create a function that receives a tuple of numbers and returns the **largest number** in that tuple.

Test the function with different tuples.

Think about what should happen if the tuple contains negative numbers as well.

---

**Q15.**
Imagine you are building a simple **product information system**.

Each product is represented by a tuple containing:

* product name
* price
* quantity

Create several product tuples and store them together in an appropriate structure.

Then process the products to calculate the **total value of all products** and display the product information.

Decide yourself how you will organize and access the tuples.